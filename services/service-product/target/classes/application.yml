spring:
  application:
    name: service-product

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:13848
      config:
        import-check:
          enabled: false

server:
  port: 9005